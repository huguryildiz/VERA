# ── Dependencies & build output ───────────────────────────────
node_modules/
dist/
.vite/
*.tsbuildinfo

# ── Test output ───────────────────────────────────────────────
test-results/
coverage/
e2e/.auth/

# ── Environment (never commit secrets) ────────────────────────
# *.example files are templates and ARE tracked.
.env.local
.env*.local
.env.test
.env.production
supabase/functions/.env
supabase/functions/.env.local

# ── Supabase local dev ────────────────────────────────────────
supabase/.temp/

# ── OS ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Logs ──────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── IDE / editor ──────────────────────────────────────────────
.vscode/
.vscode/extensions.json
.vscode/settings.json
.idea/
.obsidian/
*.swp
*.swo

# ── AI / local dev guides (intentionally per-developer) ───────
# CLAUDE.md, AGENTS.md, GEMINI.md are local AI assistant guides.
# Kept untracked so each contributor can tune them without churn.
CLAUDE.md
AGENTS.md
GEMINI.md
.agents/
.claude/
.claude/internal
.claude/rules
.superpowers/
.cursorrules
.cursor/
.copilot/
.aider*

# ── Brainstorming / spec scaffolding (local working dirs) ─────
docs/superpowers/

# ── Tooling artifacts ─────────────────────────────────────────
# Git worktree checkouts (we never use worktrees — see CLAUDE.md)
.worktrees/
# Playwright MCP session captures
.playwright-mcp/
# Graphify knowledge graph output (regeneratable, machine-specific)
graphify-out/
graph.html
graph.json
